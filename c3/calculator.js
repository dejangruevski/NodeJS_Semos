const sobiranje = (x, y) => {
    return x + y
}
const odzemanje = (x, y) => {
    return x - y
}
const mnozenje = (x, y) => {
    return x * y
}
const delenje = (x, y) => {
    return x / y
}

module.export = {
    sobiranje,
    odzemanje,
    mnozenje,
    delenje
}